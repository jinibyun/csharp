using System;
using System.Collections.Generic;
using System.Text;

namespace newVersionCSharp._6._0
{
    public class awaitWithCatch
    {
        /*
        IDbConnection conn;
        try
        {
            //...
            var response = await req.GetResponseAsync();
            //...
        }
        catch (Exception ex)
        {
            // asynchronously logging
            await Log(ex);
        }            
        finally
        {
            // asynchronously close
            await Close(conn);
        } 
        */


        /*
        // Exception Filter
        try
        {
            //...
        }
        catch(Win32Exception ex) when (ex.NativeErrorCode == 0x10)
        {
            Log(ex);
        } 
        */
    }
}
