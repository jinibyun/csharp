using System;
using System.Collections.Generic;
using System.Text;

namespace newVersionCSharp._6._0
{
    public class NullConditionalOperator: TestMain
    {
        public override void Test()
        {
            /*
            // ?. : if it is not null
           
            int? cnt = rows?.Count;

            Customer c = customers?[0];

            int? age = customers?[0]?.Age;

            // ?? : if it is null
            int cnt = rows?.Count ?? 0;
            */
        }
           
    }
}
